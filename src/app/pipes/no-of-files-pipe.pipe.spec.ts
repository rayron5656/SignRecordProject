import { NoOfFilesPipePipe } from './no-of-files-pipe.pipe';

describe('NoOfFilesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NoOfFilesPipePipe();
    expect(pipe).toBeTruthy();
  });
});
